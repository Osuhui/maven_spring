<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huiwl.dao.UserMapper">

  <insert id="addUser" parameterType="User">
    insert into
      USER_INFO(
        USER_ID,
        USER_NAME,
        USER_PWD)
    values(
      trunc(DBMS_RANDOM.value(1,10000000000)),
      #{user.name},
      #{user.password})
  </insert>

  <select id="getUserFromUserName" parameterType="String" resultMap="User">
    select *
    from
      USER_INFO
    where
      USER_NAME = #{userName}
  </select>

  <update id="updateUser" parameterType="USer">
    update
      USER_INFO
    set
      USER_NAME = #{user.name},
      USER_PWD = #{user.password}
    where
    USER_ID = #{user.id}
  </update>

  <delete id="deleteUser" parameterType="String">
    delete from 
      USER_INFO
    where USER_ID = #{userId}
  </delete>

  <select id="getUserInfo" parameterType="String" resultMap="JalanUser">
    select * from J_USR where USR_ID = #{userId}
  </select>

</mapper>